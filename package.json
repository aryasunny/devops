{
  "name": "devops-slides",
  "version": "1.0.0",
  "description": "Shift Left — Event Driven Monitoring & DevOps Automation",
  "scripts": {
    "dev": "marp -s . --html --open",
    "build": "marp deck.md --html --allow-local-files -o dist/index.html && npm run copy-assets",
    "copy-assets": "node scripts/copy-assets.js"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^3.4.0"
  }
}
