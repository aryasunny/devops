{
  "name": "event-driven-email-deck",
  "private": true,
  "scripts": {
    "dev": "marp deck.md --html --server --watch --open",
    "build": "marp deck.md --html --allow-local-files -o dist/index.html",
    "build:pdf": "marp deck.md --pdf --allow-local-files -o dist/deck.pdf"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^3.4.0"
  }
}
